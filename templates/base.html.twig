<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}{% endblock %}</title>

        <!-- Bootstrap -->
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">

        <style>
            li {
                list-style: none;
            }
            
            a {
                color: #333;
            }
        </style>
    </head>
    <body>
        <div class="header">
            <nav class="navbar navbar-dark bg-dark mt-3 mb-3 justify-content-center">
                <a href="{{ path('article_index') }}" class="navbar-brand">
                    ブログ
                </a>
                <ul class="navbar-nav mr-2">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('article_new') }}">記事投稿</a>
                    </li>
                </ul>
            </nav>
        </div>

        <div class="text-center">
            {% block body %}
            {% endblock %}
        </div>
    </body>
</html>
